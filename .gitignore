/target
**/*.rs.bk
*.db
*.db-shm
*.db-wal

# Erlang/BEAM crash dumps (transient; never commit)
erl_crash.dump

# Local agent/workflow orchestration scripts (machine-specific paths)
.workflows/

# Browser-automation artifacts
.playwright-mcp/

# Coverage report from cargo-llvm-cov (generated in CI, never committed).
/lcov.info

# TypeScript app (single-app migration)
node_modules/
/dist/
